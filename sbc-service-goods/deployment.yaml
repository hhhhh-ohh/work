apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: goods
    name: goods-deployment
  name: goods
  namespace: sbc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: goods
  template:
    metadata:
      labels:
        app: goods
    spec:
      imagePullSecrets:
        - name: docker-secret
      containers:
        - image: {{IMAGE_NAME}}
          imagePullPolicy: Always
          name: goods
          ports:
            - containerPort: 8950
              name: tomcat-port
          livenessProbe:
            httpGet:
              path: /act/health
              port: 8951
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 30
          env:
            - name: JAVA_OPTS
              value: {{JAVA_ENV_PARAMS}}
            - name: NACOS_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: sbc-config
                  key: nacosAddress
            - name: RESTART_
              value: {{DATE}}
#          resources:
#            limits:
#              cpu: '1'
#              memory: 3Gi
#            requests:
#              cpu: '1'JAVA_ENV_PARAMS
#              memory: 3Gi
          volumeMounts:
            - mountPath: /data/server/log
              name: goods-logs
      volumes:
        - name: goods-logs
          hostPath:
            path: /data/sbc/logs/goods
      hostNetwork: false
---