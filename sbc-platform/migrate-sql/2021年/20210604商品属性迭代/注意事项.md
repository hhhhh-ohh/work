-- 1、对品牌表中历史数据的排序、是否推荐进行初始化后
需要检查历史数据create_time 字段 时间格式是否正常，如不正常手动更改为当前时间
然后进行品牌es数据初始化
   `sbc-goods`.`goods_brand`

-- 2、商品es数据初始化 ，
如前端搜索操作失败，接口日志提示`goodsPropDetailNest`不是一个`nested`类型
则需要将商品es结构信息删除后再初始化商品es
商品库同理

清空索引（结构+数据）：
商品:
curl  -H "Content-Type: application/json" -XDELETE 'http://localhost:9200/es_goods'
商品sku:
curl  -H "Content-Type: application/json" -XDELETE 'http://localhost:9200/es_goods_info'

-- 3、PC端域名已经调整 原型 `/site` 已删除，需要更改nginx配置 和 boss后端配置的PC端地址
   nginx修改位置：/data/nginx/conf/dist.conf 
   删除相关联 /site 
   location / 下  alias 改为  root 
   开发环境或者其他疑问 咨询运维人员

-- 4、(打包平台)APP打包构建命令增加 yarn replace
-- 5、(jenkins mofang构建命令)魔方 x-site 配置修改

      osd setting 增加：export ssl=true  (minio 使用 http 改成 false)

      添加项目配置信息 增加 ： ssl:${ssl}