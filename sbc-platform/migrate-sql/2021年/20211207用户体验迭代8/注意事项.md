sql脚本执行之后,代码sku数据库实体及es实体中新增重量体积字段，es数据需初始化。
确保发完最新的包之后再执行es
执行 curl -XDELETE http://localhost:9200/es_goods，重新去/es-init初始化商品
执行 curl -XDELETE http://localhost:9200/es_goods_info，重新去/es-init初始化商品
执行 curl -XDELETE http://localhost:9200/es_standard_goods，重新去/es-init初始化商品库

boss端-设置-初始化es   商品和商品库初始化一下

添加下单积分增长，sbc-service-mq.properties上mq配置新增
spring.cloud.stream.bindings.deal-order-points-increase-input.destination=q.deal.order.points.increase
spring.cloud.stream.bindings.deal-order-points-increase-output.destination=q.deal.order.points.increase

x-site-magic-box  jenkins 在yarn之前脚本增加: yarn run pre-build taro