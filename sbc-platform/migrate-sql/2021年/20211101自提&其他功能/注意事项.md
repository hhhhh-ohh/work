1、本迭代包含了跨境和o2o的相关代码，前端使用flag字段控制（crossBorderFlag 、isO2oFlag），
后端使用plugin插件控制（sbc-service-plugin服务不在上线范围内，对应pom对应plugin依赖已删除）。

2、配送方式支持自提了，本迭代中自提点位置、
收货地址定位、自提点地图 还没有接入定位和地图，后续迭代会上线这些功能。

3、新增mq服务，其他服务mq相关迁移至mq服务中
   nacos对应增加 mq配置文件 sbc-service-mq.properties

4、基线产品线，需要把O2O和跨境涉及到的标品sbc的脚本执行下