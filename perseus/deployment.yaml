apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: perseus
    name: perseus-deployment
  name: perseus
  namespace: sbc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: perseus
  template:
    metadata:
      labels:
        app: perseus
    spec:
      imagePullSecrets:
        - name: docker-secret
      containers:
        - image: {{IMAGE_NAME}}
          imagePullPolicy: Always
          name: perseus
          ports:
            - containerPort: 8590
              name: tomcat-port
          livenessProbe:
            httpGet:
              path: /act/health
              port: 8591
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 30
          env:
            - name: JAVA_OPTS
              value: {{JAVA_ENV_PARAMS}}
            - name: NACOS_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: sbc-config
                  key: nacosAddress
            - name: RESTART_
              value: {{DATE}}
#          resources:
#            limits:
#              cpu: '1'
#              memory: 3Gi
#            requests:
#              cpu: '1'JAVA_ENV_PARAMS
#              memory: 3Gi
          volumeMounts:
            - mountPath: /data/server/log
              name: perseus-logs
      volumes:
        - name: perseus-logs
          hostPath:
            path: /data/sbc/logs/perseus
      hostNetwork: false
---
apiVersion: v1
kind: Service
metadata:
  name: perseus-svc
  namespace: sbc
  labels:
    k8s-app: perseus-svc
spec:
  type: ClusterIP
  ports:
    - port: 8590
      targetPort: 8590
      protocol: TCP
      name: http
  selector:
    app: perseus